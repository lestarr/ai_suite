from setuptools import setup, find_packages

setup(
    name="ai_suite",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # your dependencies here
    ]
) 

# run: pip install -e .